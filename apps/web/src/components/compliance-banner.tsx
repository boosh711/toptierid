export function ComplianceBanner() {
  return (
    <div className="border-b border-accent/20 bg-surface px-4 py-2 text-center text-xs text-muted">
      NCAA compliance rules are stubbed for this prototype — configure with counsel before launch.
    </div>
  );
}
