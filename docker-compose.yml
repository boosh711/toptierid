services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: toptier
      POSTGRES_PASSWORD: toptier
      POSTGRES_DB: toptierid
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
